//dart
//class 12-class 15

//widget build function UI.In flutter importing is essential for accessing code defined outside your current file. So it is necessary to import.
Source code might not be accessible.So import.

//Stateful widget dynamic mutable copnents that updte their appearance or behavior in real time based on user interaction data changes. 
  
//Stateless widget is a static building block in flutter that describes part of a user interface that des not change overtime.

//Material app is a high level convenience widget that serves as the entry point for applications designed using Google material design

//Core Functions: Theming,Navigation,Material Design Tools,Localization.
How to write title and body.Adjust height and width or column.Alignment
 
//Buttons and message printer,Main Axis Alignment,Image upload,dropdown list,drawer,enter text onpressed couter,navigation bar,scroll down

//Material package in flutter(package:flutter/material dart) is a core library that provides a comprehensive set of widgets implementing google design.

//theme-material app
App-will be material app

//Scaffold is a class in flutter that provides many widgets or we can say 
API.
//Example of stateful and stateless widget.
//Appbar and name should change on icon clicked.

//Stateless and Stateful widget -> build method
Placeholder => PlaceHolder()
Scaffold appbar and body
icon button in leading and action
[] arrays can add many widgets

//example onpressed message:

import 'package:flutter/material.dart';

void main() {
  runApp(const FirstOne());
}

class FirstOne extends StatelessWidget {
  const FirstOne({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: SecondOne(),
      debugShowCheckedModeBanner: false,
    );
  }
}

class SecondOne extends StatefulWidget {
  const SecondOne({super.key});

  @override
  State<SecondOne> createState() => _SecondOneState();
}

class _SecondOneState extends State<SecondOne> {
  String message = "Welcome";

  void _updateMessage(String newMessage) {
    setState(() {
      message = newMessage;
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.orange,
        title: const Text("Welcome"),
        leading: IconButton(
          onPressed: () => _updateMessage("Menu Icon Clicked"),
          icon: const Icon(Icons.menu),
        ),
        actions: [
          IconButton(
            onPressed: () => _updateMessage("Wifi Icon Clicked"),
            icon: const Icon(Icons.wifi),
          ),
          IconButton(
            onPressed: () => _updateMessage("Settings Icon Clicked"),
            icon: const Icon(Icons.settings),
          ),
        ],
      ),
      body: Center(
        child: Text(
          message,
          style: const TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
        ),
      ),
    );
  }
}

image add line
      
Image.asset("images/Screenshot.png",height: 100,width: 100,),
Image.asset("images/Screenshot.jpg",height: 100,width: 100,),
Image.asset("images/Screenshot.jpeg",height: 100,width: 100,),
Image.asset("images/Screenshot.jpg",height: 100,width: 100,),




